apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "chart.name" . }}
  namespace: {{ .Release.Namespace }}
data:
{{- range $key, $value := .Values.env.configmap }}
  {{ $key }}: {{ $value | quote }}
{{- end }}