<h1 class="mt-3">New Salary History</h1>

<%= render 'form' %>

<%= button_to "Back to Salary Histories", user_salary_histories_path(@user, @salary_history), class: "btn btn-success", method: :get %>
